# Kai Jones

Personal site and writing archive for accessibility, DeafBench, and security work.

The site is dependency-free. Serve the repository root with any static file server:

```powershell
python -m http.server 8000
```
